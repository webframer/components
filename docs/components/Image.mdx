import { CodeExample } from '@webframer/ui/views/CodeExample.js'
import PropsTable from '@webframer/ui/views/PropsTable.js'
import Image from 'view/assets/Image.js'
import AssetCard from 'view/components/AssetCard.jsx'

# Image

<AssetCard className='float-right margin' component={Image} />

A component to display graphical elements.

It is the native `img` element in the browser, which means you can pass in any props supported by the browser.

## Prop Types

<PropsTable component={Image} />

## Examples
<CodeExample desc='Using file `name` relative to `/static/images/` folder'>
  <Image name='image.png' />
</CodeExample>

<CodeExample desc='Using `src` attribute'>
  <Image src='/static/images/image.png' />
</CodeExample>

<CodeExample desc='Custom Image Style'>
  <Image className='max-width-mobile animate-bounce' name='image.png' />
</CodeExample>
